cloud:
  # 服务名称，同一个服务的负载均衡集群的name必须一致，不同集群之间必须唯一
  name: cloud-client1
  # 尽量长一点，防止接口过多来不及发布
  sessionTimeout: 10000
  # 1 轮训，2 随机，3 加权轮训，4 加权随机，5 源地址哈希法，6 最小连接
  strategy: 1
  # zookeeper地址，多个地址用英文逗号分割
  register: 10.211.55.9:2180
